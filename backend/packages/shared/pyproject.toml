[project]
name = "erp-assistant-shared"
version = "0.1.0"
description = "Shared ingestion / doc-type contract for AI ERP Assistant (Python)"
requires-python = ">=3.8"
readme = "README.md"
dependencies = []

[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
